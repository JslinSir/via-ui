export const CardDefaultProps = {

    // 圆角
    radius: {
        type: Number,
        value: 20
    },

    // 不显示头部下方边框，默认显示
    disHeaderBorder: {
        type: Boolean,
        value: false
    },

    // 不显示卡片头部，默认展示
    disHeader: {
        type: Boolean,
        value: false
    },

    // 卡片标题
    title: {
        type: String,
        value: ""
    }

}